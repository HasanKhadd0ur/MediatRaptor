namespace MediatRaptor.Mediator.Core
{
    // Executor lives in Core.Mediator currently; keep this helper for potential reuse.
    internal static class PipelineBehaviorExecutor
    {
        // Left intentionally minimal because Core.Mediator composes behaviors using reflection delegates.
    }
}
